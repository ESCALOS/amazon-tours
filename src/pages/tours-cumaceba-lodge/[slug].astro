---
import type { GetStaticPaths } from "astro";
import { getCollection } from "astro:content";
import TourItemLayout from "src/layouts/TourItemLayout.astro";

export const getStaticPaths = (async () => {
  const tours = await getCollection("cumacebaLodge");
  return tours.map((tour) => ({
    params: {
      slug: tour.slug,
    },
    props: tour,
  }));
}) satisfies GetStaticPaths;

const tour = Astro.props;

const image = {
  path: "/src/images/breadcrumb/bg-cumaceba.png",
  alt: "Iquitos Expedition - Tours Cumaceba Lodge - Iquitos - Amazonas",
  style: "object-position: 50% 28%",
};
---

<TourItemLayout image={image} sectionName="Cumaceba Lodge" tour={tour} />
