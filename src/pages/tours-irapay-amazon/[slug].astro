---
import type { GetStaticPaths } from "astro";
import { getCollection } from "astro:content";
import TourItemLayout from "src/layouts/TourItemLayout.astro";

export const getStaticPaths = (async () => {
  const tours = await getCollection("irapayAmazon");
  return tours.map((tour) => ({
    params: {
      slug: tour.slug,
    },
    props: tour,
  }));
}) satisfies GetStaticPaths;

const tour = Astro.props;

const image = {
  path: "/src/images/breadcrumb/bg-irapay.png",
  alt: "Iquitos Expedition - Tours Irapay Amazon",
  style: "object-position: 50% 28%",
};
---

<TourItemLayout image={image} sectionName="Irapay Amazon" tour={tour} />
